{
  "mcpServers": {
    "agent-looker": {
      "type": "http",
      "url": "${AGENT_LOOKER_MCP_URL:-https://api.agentlooker.ai/mcp}",
      "headers": {
        "Authorization": "Bearer ${AGENT_LOOKER_API_TOKEN}"
      }
    }
  }
}
